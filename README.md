# marc-to-csv

## Description:
A Python program to convert MARC records into UTF-8 formated CSV file.

### External Libraries (individually listed)
pymarc
`pip install pymarc`

## Install Requirments (all):
`pip install -r requirements.txt`
